export type LoginFormInput = {
    email: string,
    password: string,
};

export type CadastroFormInput = {
    nome: string,
    email: string,
    password: string,
    password_confirmacao: string,
}